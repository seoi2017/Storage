#include<iostream>
#include<cstdio>
#include<cstdlib>
#include<algorithm>
#include<cstring>
#include<cmath>
#define get_size(x) (x?x->size:0)
using namespace std;
struct treap_t{
    treap_t *l,*r;
    
};
int main(){
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    
    fclose(stdin);
    fclose(stdout);
    return 0;
}